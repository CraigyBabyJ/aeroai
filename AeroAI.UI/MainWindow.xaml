<Window x:Class="AeroAI.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AeroAI" Height="700" Width="1000"
        Background="{StaticResource BackgroundDarkBrush}"
        WindowStyle="None" AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip">
    
    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>  <!-- Title bar -->
            <RowDefinition Height="60"/>  <!-- COM/XPDR bar -->
            <RowDefinition Height="Auto"/> <!-- Airport info -->
            <RowDefinition Height="*"/>    <!-- Messages -->
            <RowDefinition Height="60"/>   <!-- Input -->
            <RowDefinition Height="50"/>   <!-- Bottom buttons -->
        </Grid.RowDefinitions>
        
        <!-- Title Bar -->
        <Border Grid.Row="0" Background="{StaticResource BackgroundMediumBrush}" 
                MouseDown="TitleBar_MouseDown">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                    <Ellipse Width="40" Height="40" Fill="{StaticResource AccentGreenBrush}" Margin="0,0,10,0"/>
                    <TextBlock Text="AeroAI" FontSize="18" FontWeight="Bold" 
                               Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    <TextBlock Text="v1.0" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" 
                               VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                    <TextBlock Text="Flight-ID:" Foreground="{StaticResource TextSecondaryBrush}" 
                               VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock x:Name="FlightIdText" Text="---" 
                               Foreground="{StaticResource AccentCyanBrush}" 
                               VerticalAlignment="Center" FontWeight="Bold"/>
                    
                    <Button Content="−" Width="30" Height="30" Margin="20,0,5,0" 
                            Style="{StaticResource NavButton}" Click="Minimize_Click"/>
                    <Button Content="×" Width="30" Height="30" 
                            Style="{StaticResource NavButton}" Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- COM/XPDR Bar -->
        <Border Grid.Row="1" Background="{StaticResource BackgroundMediumBrush}" Margin="0,2,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                <Border Background="#2a2a4a" CornerRadius="4" Padding="15,8" Margin="0,0,15,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="COM1" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0"/>
                        <TextBlock x:Name="Com1Freq" Text="121.705" FontSize="16" FontWeight="Bold"
                                   Foreground="{StaticResource AccentGreenBrush}"/>
                    </StackPanel>
                </Border>
                
                <Border Background="#2a2a4a" CornerRadius="4" Padding="15,8" Margin="0,0,15,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="COM2" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0"/>
                        <TextBlock x:Name="Com2Freq" Text="OFF" FontSize="16" 
                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Border>
                
                <Border Background="#2a2a4a" CornerRadius="4" Padding="15,8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="XPDR" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0"/>
                        <TextBlock x:Name="XpdrCode" Text="----" FontSize="16" FontWeight="Bold"
                                   Foreground="{StaticResource AccentYellowBrush}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
        
        <!-- Airport Info Panel -->
        <Border Grid.Row="2" Background="{StaticResource BackgroundMediumBrush}" Margin="10" 
                CornerRadius="8" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Left: Airport -->
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="AirportIcao" Text="EGCC" FontSize="24" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock x:Name="AirportName" Text="Manchester Airport" FontSize="14"
                                   Foreground="{StaticResource TextSecondaryBrush}" 
                                   VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                    
                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="CLR" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="ClrFreq" Text="121.705" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="GND" Foreground="{StaticResource TextSecondaryBrush}" Margin="20,0,0,0"/>
                        <TextBlock Grid.Row="0" Grid.Column="3" x:Name="GndFreq" Text="121.705" Foreground="{StaticResource AccentGreenBrush}" FontWeight="Bold"/>
                        
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="TWR" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TwrFreq" Text="118.63" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="1" Grid.Column="2" Text="DEP" Foreground="{StaticResource TextSecondaryBrush}" Margin="20,0,0,0"/>
                        <TextBlock Grid.Row="1" Grid.Column="3" x:Name="DepFreq" Text="118.58" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </Grid>
                </StackPanel>
                
                <!-- Center: Flight Info -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="Flight ID" Foreground="{StaticResource TextSecondaryBrush}" Width="80"/>
                        <TextBlock x:Name="FlightIdDisplay" Text="---" Foreground="{StaticResource AccentCyanBrush}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="Dest" Foreground="{StaticResource TextSecondaryBrush}" Width="80"/>
                        <TextBlock x:Name="DestIcao" Text="---" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Squawk" Foreground="{StaticResource TextSecondaryBrush}" Width="80"/>
                        <TextBlock x:Name="SquawkDisplay" Text="----" Foreground="{StaticResource AccentYellowBrush}"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- Right: Callsign -->
                <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <TextBlock x:Name="Callsign" Text="N123AZ" FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock x:Name="AircraftType" Text="[A319]" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Message History -->
        <Border Grid.Row="3" Margin="10,0" CornerRadius="8" Background="{StaticResource BackgroundMediumBrush}">
            <ScrollViewer x:Name="MessageScroller" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="MessageList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5" Padding="10" CornerRadius="4"
                                    Background="{Binding Background}">
                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Copy message" Click="CopyMessage_Click"
                                                  CommandParameter="{Binding}"/>
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="200"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Station}" 
                                               Foreground="{StaticResource TextSecondaryBrush}"/>
                                    <TextBox Grid.Column="1" Text="{Binding Message}" 
                                             Foreground="{Binding MessageColor}"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             IsReadOnly="True"
                                             TextWrapping="Wrap"
                                             Padding="0"
                                             AcceptsReturn="True"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
        
        <!-- Input Bar -->
        <Border Grid.Row="4" Margin="10,10,10,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Ellipse Grid.Column="0" Width="40" Height="40" Fill="{StaticResource AccentGreenBrush}" 
                         Opacity="0.3"/>
                
                <TextBox Grid.Column="1" x:Name="PilotInput" 
                         Background="#2a2a4a" Foreground="{StaticResource TextPrimaryBrush}"
                         BorderThickness="0" FontSize="14" Padding="15,10"
                         VerticalContentAlignment="Center"
                         KeyDown="PilotInput_KeyDown"/>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="10,0,0,0">
                    <Button Content="COM" Style="{StaticResource NavButton}" Margin="0,0,5,0"/>
                    <Button Content="ATC" Style="{StaticResource NavButton}" 
                            Background="{StaticResource AccentGreenBrush}" Foreground="Black"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Bottom Buttons -->
        <Border Grid.Row="5" Margin="10,5,10,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="New Flight" Style="{StaticResource NavButton}" Margin="0,0,10,0"
                        Click="NewFlight_Click"/>
                <Button Content="Settings" Style="{StaticResource NavButton}" Margin="0,0,10,0"/>
                <Button Content="SimBrief" Style="{StaticResource NavButton}" Margin="0,0,10,0"
                        Click="SimBrief_Click"/>
                <Button Content="Import Flight Plan" Style="{StaticResource NavButton}" Margin="0,0,10,0"
                        Click="ImportFlightPlan_Click"/>
                <Button Content="Copy last ATC" Style="{StaticResource NavButton}" Margin="0,0,10,0"
                        Click="CopyLastAtc_Click"/>
                <Button Content="About" Style="{StaticResource NavButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
